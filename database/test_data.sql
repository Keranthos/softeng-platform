-- 本文件已合并到 seed_demo.sql（固定主键、外键一致，适合演示）。
-- 导入演示数据请执行：
--   mysql -u root -p softeng < database/seed_demo.sql
-- 旧库若缺列请先执行：mysql -u root -p softeng < database/patch_existing_to_demo.sql

SELECT '请改用 database/seed_demo.sql 导入演示数据' AS notice;
